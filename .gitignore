# Python
__pycache__/
*.pyc
.pytest_cache/

# local evaluation server data (SQLite + monitors.toml with tokens)
server/data/
server/config/monitors.toml

# local monitor SQLite (when run manually outside ~/.local/share)
*.db
*.db-wal
*.db-shm

# local email configuration (SMTP credentials) — never commit!
/.env
server/.env

# --- leftovers of the bash version (data on the measured hosts) ---
log/
/archiv/
/report.html
/netmon.run.log
/netmon.pid
/latency.csv
/speed.csv
/reach.csv
/uptime.csv
/events.csv
